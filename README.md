# Frontend Dashboard Assignment

A small React + Redux Toolkit app that satisfies the trainee assignment.

## Run locally
1. `npm install`
2. `npm run dev`
3. Open the URL Vite prints (e.g. http://localhost:5173)

Tested on Node 18/20. Use PowerShell or Git Bash on Windows.